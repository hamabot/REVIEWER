<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:backgroundTint="#696969"
    android:backgroundTintMode="multiply"
    tools:context=".MainActivity">

    <!-- ここにToolBarを定義 -->
    <android.support.v7.widget.Toolbar
        android:id="@+id/maintool_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:elevation="5dp"
        android:minHeight="?attr/actionBarSize"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar">
    </android.support.v7.widget.Toolbar>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:gravity="center"
        android:orientation="horizontal">

        <EditText
            android:id="@+id/et"
            android:layout_width="250dp"
            android:layout_height="50dp"
            android:background="@drawable/shape_rouded_corners_5dp"
            android:hint="@string/search_init_text"
            android:inputType="text"
            android:textColor="@color/white"/>

        <ImageButton
            android:id="@+id/btn"
            android:layout_width="60dp"
            android:layout_height="50dp"
            android:background="@drawable/shape_rouded_corners_5dp2"
            android:src="@drawable/search_ic_mini"/>

    </LinearLayout>

</RelativeLayout>
